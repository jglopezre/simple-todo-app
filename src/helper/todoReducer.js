export const todoReducer = (initialState = [], action = {}) => {
  let stateBuffer = [];
  switch (action.type) {
    case value:
      
      break;
  
    default:
      stateBuffer = initialState;
      break;
  }
  return stateBuffer
}